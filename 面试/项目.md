# 拷打项目/实习时出现的问题

## 商品余额统计

- 

- 亲缘线程池是用在哪个环节的？
  - 消费者拿到数据后，会将数据放到阻塞队列里等待聚合（BufferTrigger），聚合计算之后更新数据库，为了保证效率，更新数据库时使用了线程池
  - 使用亲缘线程池，是为了保证让相同属性的任务每次都被同一线程执行，这样的话可以避免数据库层面锁的竞争。
  - 如果使用的是普通线程池，会出现多个线程并发更新同一个商家数据的问题，就涉及到了锁的竞争。
